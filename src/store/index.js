import Vue from 'vue'
import Vuex from 'vuex'
import initialState from './initialState'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    initialState
  },
  mutations: {
  },
  actions: {
  },
  
})
